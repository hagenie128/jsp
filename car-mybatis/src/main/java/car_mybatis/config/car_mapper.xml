<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="car_mybatis.mapper.CarMapper">	
	<select id="selectAllCar" resultType="car_mybatis.dto.CarDTO">
		select * from cars
	</select>
</mapper>