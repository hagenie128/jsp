<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mapper.MemberMapper">
	<select id="selectAllMember" resultType="member">
		select * from
		board_member
	</select>
	<insert id="insertMember">
		insert into
		board_member(id, passwd, username, nickname)
		values(#{id}, sha2(#{passwd},512),
		#{userName}, #{nickName})
	</insert>
	<select id="selectMemberById" resultType="member">
		select * from
		board_member where id = #{id}
	</select>
	<select id="login" resultType="member">
		select 
			no,username,nickname
		from 
			board_member 
		where
			id = #{id} and passwd = sha2(#{passwd},512)
	</select>
	<select id="search" resultType="member">
		<!-- kind에 있는 컬럼명으로 search 값을 이용해서 부분일치 검색 -->
		select * from
		board_member where ${kind} like concat('%',#{search},'%')
	</select>
	<delete id="delete">
		delete from board_member where no=#{no}
	</delete>
</mapper>